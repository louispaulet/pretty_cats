# Agent Instructions

- Read this file before starting any task in this repository.
- Keep changes scoped to the request and avoid overwriting user work.
- Always create a commit and push it, even when working on `main`.
- If the work is complete, push the change to `origin/main` unless the user asks otherwise.
